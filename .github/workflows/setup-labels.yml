name: Setup Labels

on:
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  setup:
    uses: TomzxCode/llm-augmented-workflows/.github/workflows/setup-labels.yml@generic-workflows
    secrets: inherit
